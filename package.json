{
  "name": "heritage-qc",
  "version": "1.0.0",
  "description": "Quality control and annotation system for heritage collections",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "validate-config": "bun run src/config/validate-config.ts",
    "generate-types": "bunx json-schema-to-typescript schemas/config-schema.json -o schemas/config-schema.ts"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "shell-quote": "^1.8.1",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "json-schema-to-typescript": "^13.1.1"
  }
}
